#while  + true + break para quebra o loop
#soma = 0
#while True:
    #n = int(input(("Digite um numero qualquer: ")))
    #if n == 999:
        #break
   # soma += n
#F e o novo .format() so que mais simples 
#print(f"a soma dos numeros digitados foi {soma}")